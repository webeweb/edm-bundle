# Resources/config/services/filesystem_storage_manager.yml
parameters:

    webeweb.edm.directory: "%kernel.root_dir%/data/edm"

services:

    webeweb.edm.provider.storage.filesystem:
        class:     WBW\Bundle\EDMBundle\Provider\FileSystemStorageProvider
        arguments: ["%webeweb.edm.directory%"]
        tags:
            - { name: "webeweb.edm.storage.provider" }
